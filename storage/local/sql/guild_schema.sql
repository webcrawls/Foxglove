CREATE TABLE IF NOT EXISTS guilds (guild_id TEXT PRIMARY KEY,
                                                     created TEXT,
                                                     settings TEXT,
                                                     meta TEXT,
                                                     users TEXT);